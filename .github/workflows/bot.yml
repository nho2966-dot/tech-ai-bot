name: Tech AI Bot Runner

on:
  schedule:
    - cron: '0 */2 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install
        run: pip install tweepy openai pyyaml

      - name: Run Bot (Smart Execution)
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: |
          # العثور على المسار الحقيقي للملف وتشغيله
          FILE_PATH=$(find . -name "main.py" | head -n 1)
          if [ -z "$FILE_PATH" ]; then
            echo "❌ الملف غير موجود!"
            exit 1
          fi
          echo "🚀 جاري التشغيل من: $FILE_PATH"
          # الدخول لمجلد الملف لضمان قراءة config.yaml أو state.json
          cd $(dirname "$FILE_PATH")
          python main.py

      - name: Save State
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "action@github.com"
          git pull --rebase origin main
          git add **/state.json || echo "No changes"
          git commit -m "update state" || echo "nothing"
          git push origin main
