name: Tech AI Bot Runner

on:
  schedule:
    - cron: '0 */2 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: pip install tweepy openai pyyaml

      - name: Run Bot (Smart Search Mode)
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
        run: |
          # ابحث عن المسار الحقيقي لملف main.py
          MAIN_PATH=$(find . -name "main.py" | head -n 1)
          
          if [ -z "$MAIN_PATH" ]; then
            echo "❌ لم يتم العثور على main.py في أي مكان!"
            exit 1
          fi
          
          echo "🚀 جاري تشغيل البوت من المسار الحقيقي: $MAIN_PATH"
          
          # استخراج المجلد الذي يحتوي على الملف والدخول إليه
          DIR_PATH=$(dirname "$MAIN_PATH")
          cd "$DIR_PATH"
          
          # تشغيل البوت
          python main.py

      - name: Save State
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "action@github.com"
          git add **/state.json || echo "No state file yet"
          git commit -m "🔄 Update State" || echo "Nothing to commit"
          git push
