name: Sovereign AI Bot Cycle
on:
  schedule:
    - cron: '0 * * * *' # تشغيل كل ساعة
  workflow_dispatch:

permissions:
  contents: write # ضروري للسماح للبوت بحفظ الذاكرة

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run Sovereign Bot
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
          X_BEARER_TOKEN: ${{ secrets.X_BEARER_TOKEN }}
          GEMINI_KEY: ${{ secrets.GEMINI_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: python main.py

      - name: Commit and Push Memory Update
        run: |
          git config --local user.email "bot@nasser-ai.com"
          git config --local user.name "Nasser-AI-Bot"
          git add data/sovereign_2026.db
          git commit -m "🧠 تحديث ذاكرة البوت: تم حفظ التفاعلات الجديدة" || echo "لا يوجد تغييرات لحفظها"
          git push
