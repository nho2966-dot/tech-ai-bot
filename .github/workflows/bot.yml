name: build-and-run

on:
  schedule:
    # يعمل 4 مرات في اليوم (الساعة 12 صباحاً، 6 صباحاً، 12 ظهراً، 6 مساءً بالتوقيت العالمي)
    - cron: '0 */6 * * *' 
  workflow_dispatch: # يترك لك خيار التشغيل اليدوي عند الحاجة

jobs:
  build-and-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install tweepy feedparser google-genai python-dotenv openai

      - name: Run bot
        run: python main.py
        env:
          GEMINI_KEY: ${{ secrets.GEMINI_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          X_BEARER_TOKEN: ${{ secrets.X_BEARER_TOKEN }}
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}

      - name: Commit Database Changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add news.db
          git commit -m "🛡️ Update local database" || echo "No changes to commit"
          git push
