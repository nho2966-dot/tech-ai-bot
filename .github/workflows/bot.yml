name: TechElite Muscat Sovereign Engine

on:
  schedule:
    # تم ضبط المواعيد لتتوافق مع (9ص، 1ظ، 8م، 11م) بتوقيت مسقط
    # UTC = GST - 4
    - cron: '0 5 * * *'  # 09:00 AM Muscat
    - cron: '0 9 * * *'  # 01:00 PM Muscat
    - cron: '0 16 * * *' # 08:00 PM Muscat
    - cron: '0 19 * * *' # 11:00 PM Muscat
  workflow_dispatch: # للتشغيل اليدوي في أي وقت

jobs:
  build:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write # للسماح بحفظ الذاكرة التحريرية (.db)

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install tweepy feedparser openai python-dotenv

      - name: Run Sovereign Engine
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
          X_BEARER_TOKEN: ${{ secrets.X_BEARER_TOKEN }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: python main.py

      - name: Sync Muscat Enterprise Memory
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # إضافة قاعدة البيانات مهما كان اسمها
          git add *.db || echo "No DB changes"
          
          # الحفظ فقط في حال وجود بيانات جديدة (مثل بصمة خبر جديد)
          if ! git diff-index --quiet HEAD; then
            git commit -m "🛡️ Muscat Memory Sync [skip ci]"
            git push
          else
            echo "No new updates in this cycle."
          fi
