name: tech-elite-bot-3-daily

on:
  schedule:
    # النشر 3 مرات يومياً بتوقيت السعودية (KSA)
    # 10:00 UTC = 1:00 PM KSA
    # 17:30 UTC = 8:30 PM KSA
    # 20:30 UTC = 11:30 PM KSA
    - cron: '0 10 * * *'
    - cron: '30 17 * * *'
    - cron: '30 20 * * *'
  workflow_dispatch: # للتشغيل اليدوي عند الحاجة

permissions:
  contents: write

jobs:
  build-and-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install tweepy feedparser google-genai python-dotenv openai

      - name: Run Bot
        env:
          X_BEARER_TOKEN: ${{ secrets.X_BEARER_TOKEN }}
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
          GEMINI_KEY: ${{ secrets.GEMINI_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: python main.py

      - name: Save Database Changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add news.db
          git commit -m "💾 حفظ حالة النشر [skip ci]" || exit 0
          git push origin main
