name: TechElite Bot Runner 2026

on:
  schedule:
    # يعمل كل 6 ساعات لضمان تغطية الأخبار والتقارير
    - cron: '0 */6 * * *'
  workflow_dispatch: # للتشغيل اليدوي عند الحاجة

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write # ضروري للسماح للبوت بتحديث قاعدة البيانات

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # جلب كامل التاريخ لضمان نجاح الـ Rebase

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install tweepy feedparser openai python-dotenv

      - name: Run TechElite Bot
        env:
          X_BEARER_TOKEN: ${{ secrets.X_BEARER_TOKEN }}
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: python main.py

      - name: Sync and Push Database
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "TechElite-Bot"
          
          # إضافة قاعدة البيانات فقط
          git add news_master_2026.db system_master.log
          
          # بروتوكول المزامنة الذكية:
          # 1. جلب التغييرات الأخيرة من السيرفر
          git fetch origin main
          # 2. دمج التغييرات المحلية فوق التغييرات البعيدة لتجنب الرفض
          git rebase origin/main
          
          # 3. الرفع النهائي
          git commit -m "🛡️ Update DB and Logs [skip ci]" || echo "No changes to commit"
          git push origin main
