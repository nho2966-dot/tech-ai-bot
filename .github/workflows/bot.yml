name: Nasser AI Sovereign Cycle
on:
  schedule:
    - cron: '0 */3 * * *' # تشغيل كل 3 ساعات (للالتزام بصمام الأمان)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          
      - name: Install Dependencies
        run: pip install -r requirements.txt
        
      - name: Run Nasser Bot
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
          X_BEARER_TOKEN: ${{ secrets.X_BEARER_TOKEN }}
          GEMINI_KEY: ${{ secrets.GEMINI_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: python main.py
        
      - name: Commit Memory Changes
        run: |
          git config --local user.email "bot@nasser-ai.com"
          git config --local user.name "Nasser-AI-Bot"
          git add data/sovereign_2026.db
          git commit -m "🧠 حفظ الذاكرة التقنية الجديدة" || echo "لا تغييرات"
          git push
