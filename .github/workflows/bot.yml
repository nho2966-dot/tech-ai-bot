name: Tech Elite Bot CI

on:
  schedule:
    # التشغيل 3 مرات يومياً بتوقيت السعودية (تقريباً 9ص، 4ع، 9م)
    - cron: '0 6,13,18 * * *'
  workflow_dispatch: # للتشغيل اليدوي في أي وقت

jobs:
  build-and-run:
    runs-on: ubuntu-latest
    
    # تصريح الصلاحيات للكتابة في المستودع (مهم لحفظ الـ db)
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install tweepy feedparser google-genai python-dotenv openai flask requests

      - name: Run Tech Bot
        env:
          # مفاتيح الذكاء الاصطناعي
          GEMINI_KEY: ${{ secrets.GEMINI_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          
          # مفاتيح منصة X
          X_BEARER_TOKEN: ${{ secrets.X_BEARER_TOKEN }}
          TWITTER_API_KEY: ${{ secrets.TWITTER_API_KEY }}
          TWITTER_API_SECRET: ${{ secrets.TWITTER_API_SECRET }}
          TWITTER_ACCESS_TOKEN: ${{ secrets.TWITTER_ACCESS_TOKEN }}
          TWITTER_ACCESS_TOKEN_SECRET: ${{ secrets.TWITTER_ACCESS_TOKEN_SECRET }}
        run: python main.py

      - name: Save Database Changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add news.db
          # نستخدم || exit 0 لمنع فشل الـ Action إذا لم يكن هناك تغيير في القاعدة
          git commit -m "🛡️ Update News Database [skip ci]" || exit 0
          git push
