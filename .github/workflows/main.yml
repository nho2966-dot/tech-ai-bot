name: Tech AI Bot Final Execution

on:
  schedule:
    - cron: '0 */6 * * *' # يعمل كل 6 ساعات
  workflow_dispatch: # للتشغيل اليدوي

jobs:
  build-and-run:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          cd tech-ai-bot
          python -m pip install --upgrade pip
          pip install tweepy openai python-dotenv

      - name: Run Tech AI Bot
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          DRY_RUN: 0
        run: |
          cd tech-ai-bot
          # تشغيل الملف المُنظف الذي أرسلته لك في الرد السابق
          python main.py

      - name: Commit and Push Changes
        if: always()
        run: |
          cd tech-ai-bot
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # إضافة الملفات في حال تم إنشاؤها أو تحديثها
          git add state.json audit_log.jsonl 2>/dev/null || true
          
          # التحقق إذا كان هناك تغييرات فعلاً قبل تنفيذ الـ commit
          if [ -n "$(git status --porcelain)" ]; then
            git commit -m "🔄 Update bot state and logs [skip ci]"
            git push
          else
            echo "No changes to commit"
          fi
