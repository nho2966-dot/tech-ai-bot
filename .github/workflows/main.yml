name: Tech AI Premium Bot

on:
  schedule:
    - cron: '0 * * * *'  # يعمل رأس كل ساعة (24 مرة في اليوم)
  workflow_dispatch:      # زر التشغيل اليدوي للتجربة الفورية

jobs:
  build-and-run:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          # إضافة pytz للتعامل مع توقيت عُمان
          pip install tweepy requests python-dotenv pytz

      - name: Run Python Script
        env:
          # تأكد أن هذه الأسماء مطابقة لما في GitHub Secrets
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
        run: |
          echo "🚀 تشغيل نظام المحلل التقني والردود الذكية..."
          # استخدام -u لضمان ظهور الـ Logs فوراً للـوُضُـوح
          python -u tech-ai-bot/main.py
