name: Tech AI Bot Runner

# إعدادات وقت التشغيل
on:
  schedule:
    - cron: '0 */6 * * *' # يعمل تلقائياً كل 6 ساعات
  workflow_dispatch:      # يتيح لك التشغيل اليدوي للتجربة بنقرة زر من تبويب Actions

jobs:
  build-and-run:
    runs-on: ubuntu-latest
    
    steps:
      # 1. جلب الكود من المستودع
      - name: Checkout Code
        uses: actions/checkout@v4

      # 2. إعداد بيئة بايثون
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # 3. تثبيت المكتبات المطلوبة
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install tweepy requests python-dotenv

      # 4. تشغيل البوت وتمرير مفاتيح API
      - name: Run Python Script
        env:
          # تأكد من إضافة هذه الأسماء في إعدادات GitHub Secrets
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
        run: |
          echo "🚀 بدء عملية البحث والتشغيل بنمط الـوُضُـوح..."
          if [ -f "tech-ai-bot/main.py" ]; then
            # استخدام -u لضمان ظهور رسائل logging.info فوراً في السجلات
            python -u tech-ai-bot/main.py
          else
            echo "❌ خطأ: لم يتم العثور على الملف في المسار tech-ai-bot/main.py"
            ls -R
            exit 1
          fi
