name: Tech AI Bot Runner

on:
  schedule:
    - cron: '0 */6 * * *' # نشر تلقائي كل 6 ساعات
  workflow_dispatch:      # زر تشغيل يدوي للتجربة

jobs:
  build-and-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install tweepy requests python-dotenv

      - name: Run Python Script
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
        run: |
          echo "🔍 بدء عملية البحث والتشغيل..."
          
          # التحقق من وجود الملف في المجلد الرئيسي أو المجلد الفرعي
          if [ -f "main.py" ]; then
            echo "✅ تم العثور على main.py في المجلد الرئيسي."
            python main.py
          elif [ -f "tech-ai-bot/main.py" ]; then
            echo "✅ تم العثور على main.py في مجلد tech-ai-bot."
            python tech-ai-bot/main.py
          else
            echo "❌ خطأ: لم يتم العثور على ملف main.py في أي مكان!"
            echo "هذه هي الملفات المتاحة في مستودعك حالياً:"
            ls -R
            exit 1
          fi
